query GetUserSettings($id: String!) {
    getUserSettings(id: $id) {
        errors {
            method
            message
            field
        }
        user {
            id
            name
            email
            picure
            role {
                id
                name
                code
            }
        }
    }
}
